{
  "name": "utoo-lint-workspace",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "bench:generate": "pnpm --dir benchmarks generate",
    "bench": "pnpm --dir benchmarks bench",
    "bench:chart": "pnpm --dir benchmarks chart"
  }
}
